[Unit]
Description=Delays activation of nrsysmond
After=systemd-hostnamed.service base-config.service
Requires=systemd-hostnamed.service

[Timer]
# Time to wait after booting before we run first time
OnActiveSec=60
Unit=newrelic-sysmond.service

[Install]
WantedBy=multi-user.target